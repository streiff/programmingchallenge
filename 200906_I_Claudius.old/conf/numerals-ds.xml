<?xml version="1.0" encoding="UTF-8"?>
<datasources>
    <local-tx-datasource>

        <jndi-name>NumeralsDS</jndi-name>

        <connection-url>jdbc:hsqldb:${jboss.server.dir}${/}cust_data{/}numerals</connection-url>

        <!-- The driver class -->
        <driver-class>org.hsqldb.jdbcDriver</driver-class>

        <!-- The login and password -->
        <user-name>sa</user-name>
        <password></password>

        <min-pool-size>5</min-pool-size>
        <max-pool-size>20</max-pool-size>
        <idle-timeout-minutes>0</idle-timeout-minutes>
        <track-statements/>
        <prepared-statement-cache-size>32</prepared-statement-cache-size>

        <metadata>
            <type-mapping>Hypersonic SQL</type-mapping>
        </metadata>

        <!-- When using in-process (standalone) mode -->
        <depends>jboss:service=Hypersonic,database=numeralsDB</depends>
    </local-tx-datasource>

    <!-- For hsqldb accessed from jboss only, in-process (standalone) mode -->
    <mbean code="org.jboss.jdbc.HypersonicDatabase"
           name="jboss:service=Hypersonic,database=numeralsDB">
        <attribute name="Database">numeralsDB</attribute>
        <attribute name="InProcessMode">true</attribute>
    </mbean>
</datasources>
